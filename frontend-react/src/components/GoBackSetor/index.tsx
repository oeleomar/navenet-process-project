import * as Styled from "./styles";
import { ArrowSmLeft } from "@styled-icons/heroicons-outline/ArrowSmLeft";

export const GoBackSetor = () => {
  return (
    <Styled.Wrapper>
      <ArrowSmLeft size={32} />
    </Styled.Wrapper>
  );
};
