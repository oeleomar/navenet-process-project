import styled, { css } from "styled-components";

export const Nav = styled.nav`
  ${({ theme }) => css`
    margin-top: 1px;
  `}
`;

export const Ul = styled.ul`
  ${({ theme }) => css``}
`;
